Return ONLY valid JSON (no markdown/backticks). Output MUST be a complete JSON object.

Task: generate {{tip_count}} personalized savings tips from the input.

Output JSON object exactly with keys:
- schema_version: "1.0"
- period: copy input.period
- currency: copy input.user_context.currency
- tips: array length {{tip_count}}
- totals: { "estimated_monthly_savings_total": number } (sum of tips[].estimated_monthly_savings)
- meta: { "generated_by": "gemini", "fallback_used": false }

Each tips[i] object must have:
- id: "tip_1"..."tip_{{tip_count}}"
- title: string
- recommendation: string (1-2 sentences)
- estimated_monthly_savings: number >= 0
- confidence: number 0..1
- category_targets: array of strings
- evidence: object or null with keys { based_on, merchant, current_monthly_spend, assumption }
  - based_on MUST be an array (even if one item), e.g. ["top_merchants"]
  - each based_on value must be from: ["top_merchants","category_totals","silent_spenders","recurring_merchants"]
- nudge: string or null

Keep the response compact:
- Keep title/recommendation/nudge short.
- Prefer evidence=null unless a single short assumption is needed.

Use only categories/merchants present in the input. If income is missing, keep estimates conservative.

Input:
{{input_json}}
